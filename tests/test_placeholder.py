"""
Placeholder test module.

This exists so that `pytest tests/` has at least one test to collect and
the CI workflow (unittests.yml) passes on the initial project scaffold.
Replace/remove this once real tests are added in Task 2 (e.g. tests for
data loading, log-return calculation, change point model utilities).
"""


def test_placeholder():
    """Trivial sanity check, always passes."""
    assert True